# 测试夹具

`runtime_v0_2_0/` 是从 `v0.2.0` 发布内容逐文件保存的冻结运行时，用于验证稳定控制层、
运行时摘要和回退协议与该历史版本兼容。目录内的版本号、平台说明和 Python 要求描述的是
历史版本，不代表当前项目文档。

不要格式化或直接修改该目录；任何字节或可执行位变化都会改变发布摘要。新增兼容基线时，
请创建新的版本目录，并同步更新 ABI/摘要测试中的版本固定值。
